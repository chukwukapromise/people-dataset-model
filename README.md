# PyTorch YOLO Object Detection Model

YOLO Object Detection – PyTorch Training on Custom Dataset

I created a custom dataset and trained a YOLO object detection model in PyTorch using a minimal custom dataset (10 classes, 1 annotated image). This repository serves as a clean starter template for building and training YOLO models on small or large datasets.

📌 Features

✔ Train YOLO object detection using PyTorch

✔ Supports custom datasets (YOLO-format annotations)

✔ Example dataset: 10 classes, 1 image, 1 annotation file

✔ Easy-to-configure dataset.yaml

✔ Training script with customizable hyperparameters

✔ Export and evaluate trained model.
